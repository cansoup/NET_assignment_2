<UserControl x:Class="DineConnect.App.Views.MyFavoritesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DineConnect.App.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Padding="20" Margin="0,0,10,0" BorderBrush="#e5e7eb" BorderThickness="1" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Add New Favorite" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>

                <TextBlock Text="Name" FontWeight="Medium" Margin="0,0,0,5"/>
                <TextBox Margin="0,0,0,15" Padding="5"/>

                <TextBlock Text="Cuisine" FontWeight="Medium" Margin="0,0,0,5"/>
                <TextBox Margin="0,0,0,15" Padding="5"/>

                <TextBlock Text="Rating" FontWeight="Medium" Margin="0,0,0,5"/>
                <ComboBox Margin="0,0,0,15" Padding="5">
                    <ComboBoxItem Content="5 Stars ⭐⭐⭐⭐⭐"/>
                    <ComboBoxItem Content="4 Stars ⭐⭐⭐⭐"/>
                    <ComboBoxItem Content="3 Stars ⭐⭐⭐"/>
                    <ComboBoxItem Content="2 Stars ⭐⭐"/>
                    <ComboBoxItem Content="1 Star ⭐"/>
                </ComboBox>

                <Button Content="Add to Favorites" HorizontalAlignment="Stretch" Padding="10" Background="#4f46e5" Foreground="White" FontWeight="Bold" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- 1: List of Favorites -->
        <ListView Grid.Column="1" Margin="10,0,0,0" BorderThickness="1" BorderBrush="#e5e7eb">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Cuisine" Width="150" DisplayMemberBinding="{Binding Cuisine}"/>
                    <GridViewColumn Header="Rating" Width="100" DisplayMemberBinding="{Binding Rating}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
